Flask==3.0.0
requests==2.34.0
cryptography==41.0.5
python-dotenv==1.0.0
psutil==5.9.6

# Platform-specific dependencies
pywin32==305; sys_platform == 'win32'

# Build tools (for development only)
PyInstaller==6.3.0
