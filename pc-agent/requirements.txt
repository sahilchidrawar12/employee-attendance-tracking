Flask==3.0.0
# pywin32==305  # Windows only - handled conditionally in code
requests==2.34.0
cryptography==41.0.5
python-dotenv==1.0.0

# Platform-specific dependencies
pywin32==305; sys_platform == 'win32'
psutil==5.9.6  # Cross-platform process and system utilities
